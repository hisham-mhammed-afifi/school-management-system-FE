# School Management System - Backend

## Project Context

This is the production-ready backend for the School Management System.
The frontend is being built separately at: /absolute/path/to/frontend ← REPLACE WITH YOUR ACTUAL PATH

## Role

When Claude Code runs in this project, it acts as the **Backend Expert**.

- Answer any questions about API contracts, models, and business logic
- Generate TypeScript interfaces for the frontend when asked
- Do NOT modify backend code unless explicitly asked

## Cross-Project Reference

The frontend project maintains reference docs at:

- `/absolute/path/to/frontend/docs/backend-reference.md` - API reference consumed by frontend
- `/absolute/path/to/frontend/docs/frontend-reference.md` - Frontend architecture

If asked to update the backend reference, write to the frontend project's docs folder.
